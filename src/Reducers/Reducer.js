const initialState = {
  data: [],
  isLoading: false,
};

const Weather = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default Weather;
